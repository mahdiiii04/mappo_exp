seed: 0
experiment_name: "NSExclusive_NoPhase"
sub_exp: "MAPPO"

train:
  num_epochs: 20
  minibatch_size: 4096
  lr: 1e-4
  max_grad_norm: 10.0
  device: ???           

env:
  max_steps: 100
  scenario_name: "ns_exclusive_nav" 
  device: ???           
  num_envs: ??         

model:
  shared_params: False
  centralised_critic: True

collector:
  frames_per_batch: 60_000  
  n_iters: 200
  total_frames: ???         

buffer:
  memory_size: ???           

loss:
  gamma: 0.99
  lmbda: 0.95
  entropy_eps: 0.01
  clip_epsilon: 0.2

eval:
  frequency: 10

logger: {}

hydra:
  run:
    dir: ./outputs/${experiment_name}
  output_subdir: .hydra